<UserControl x:Class="prbd_2324_a03.View.OperationCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:prbd_2324_a03.View"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="310">
    <Grid>
        <Border Background="LightGray" CornerRadius="10" Padding="5">
            <Grid>
                <!-- Define two columns -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left StackPanel with two TextBlocks -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Operation" FontWeight="Bold"/>
                    <TextBlock >
                    <Run Text="Paid by XY"/>
                    <Run Text="" FontWeight="DemiBold"/>
                    </TextBlock> 
                </StackPanel>

                <!-- Right StackPanel with two TextBlocks -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Price" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Text="date operation" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
